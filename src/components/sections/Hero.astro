---
import { Image } from "astro:assets";
import Button from "../shared/Button.astro";
import Container from "../shared/Container.astro";
import Paragraph from "../shared/Paragraph.astro";
import ByNumber from "./ByNumber.astro";

import myHeroImage1 from "../../assets/images/hero1.jpg";
import myHeroImage2 from "../../assets/images/hero2.jpg";
import myHeroImage3 from "../../assets/images/Jimmy1.jpeg";
import myNewHeroImage from "../../assets/images/Jimmy1.jpeg";
import CtaButton from "../shared/CtaButton.astro";
import ByResult from "./ByResult.astro";
import GradiantText from "../shared/GradiantText.astro";

export interface Props {
  className?: string;
}

const { className } = Astro.props;
---

<section class={`relative pt-32 lg:pt-36 ${className}`}>
  <Container className={"flex flex-col lg:flex-row gap-10 lg:gap-12"}>
    <div
      class="relative flex flex-col items-center text-center lg:text-left lg:py-7 xl:py-8
        lg:items-start lg:max-w-none max-w-3xl mx-auto lg:mx-0 lg:flex-1 lg:w-1/2"
    >
      <h1
        class="text-3xl/tight sm:text-4xl/tight md:text-5xl/tight xl:text-6xl/tight
             font-bold text-heading-1" style="display: inline; margin: 0; padding: 0; line-height: 1;"
      >
        Welcome To<br />
        <GradiantText>Ja</GradiantText>mes <GradiantText>Pa</GradiantText>ul<br />
        <GradiantText>N</GradiantText>yaga <GradiantText>M</GradiantText>buru's<br />
        Portfolio
      </h1>
      <Paragraph className="mt-8">
        Hello, my friends call me Jimmy. Let's walk through some of the work I have done together.
      </Paragraph>

      <div class="mt-16 w-full flex flex-col">
        <div class="lg:mx-auto mb-5">
          <CtaButton />
        </div>
        <div class="mx-auto">
          <ByResult />
        </div>
      </div>
    </div>

    <div
      class="flex flex-1 lg:w-1/2 lg:h-auto relative lg:max-w-none lg:mx-0 mx-auto max-w-3xl"
    >
      <Image
        src={myHeroImage3}
        alt="Hero image"
        class="lg:absolute lg:w-full lg:h-full rounded-3xl object-cover lg:max-h-none max-h-96"
        width="2350"
        height="2359"
      />
    </div>
  </Container>

  <ByNumber />
</section>
